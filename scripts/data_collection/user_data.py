import requests
import time
import pandas as pd

def get_user_data():
    #iterate through contest_data directory

        #open file
        #iterate through file
        #check if match name is in :D

        #[handle, id, [contests_ids], [ratings]]
        #external dictionary may be very useful for efficiency w/ pandas
    print()

if __name__ == "__main__":
    get_user_data()